#!/bin/sh
log="$OMATALK_TEST_LOG"
echo "start $$" >> "$log"
trap 'echo "killed $$" >> "$log"; exit 0' TERM
ticks=$(cat "$OMATALK_TEST_TICKS_FILE" 2>/dev/null || echo 1)
i=0
while [ "$i" -lt "$ticks" ]; do
  sleep 0.05
  i=$((i + 1))
done
echo "end $$" >> "$log"
